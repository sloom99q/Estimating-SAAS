/**
 * Crisp, restrained corner radii (no pill shapes on large containers).
 * defaultRadius is set to `sm` (6px) in the theme; cards step up to `md` (8px).
 */
export const radius = {
  xs: '4px',
  sm: '6px',
  md: '8px',
  lg: '12px',
  xl: '16px',
} as const
